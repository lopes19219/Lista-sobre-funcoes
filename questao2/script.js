function calcularAreaTriangulo(){
    let base = Number (prompt('Digite a base'));
    let altura = Number (prompt('Digite a altura'));
    let area = (base * altura) / 2;
    document.writeln('A área do triângulo é: ' + area);
}
